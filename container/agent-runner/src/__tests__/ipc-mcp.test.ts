import { describe, it, expect } from 'vitest';

describe('ipc-mcp - send_file tool', () => {
  it.todo('validates file exists before writing IPC');
  it.todo('rejects paths outside /workspace/group/');
  it.todo('rejects files larger than 50MB');
  it.todo('writes correct IPC JSON matching IpcFileMessage interface');
});
